---
probes:
- path: "/test"
  rules:
  - type: "filter"
    key: "cassandra"
    operator: "in"
  - type: "filter"
    key: "disk"
    operator: "not-in"
  action:
    type: "script"
    command: "ls -lah"
